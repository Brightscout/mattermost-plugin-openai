export type ChatProps = {

    /**
     * Chat to be displayed
     */
    chat: string;

    /**
     * If `true`, custom styles will be applied to indicate that the chat was send by the user.
     */
    isUser: boolean;
};
