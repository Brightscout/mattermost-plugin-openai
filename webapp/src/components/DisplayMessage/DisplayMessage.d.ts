export type DisplayMessageProps = {

    /**
     * Message to be be displayed in the component.
     */
    message: string;

    /**
     * If `true` error styles are applied.
     */
    isError?: boolean;
};
